# 📱 Android Project: Simple JSON App

Легкий проект на Kotlin + Jetpack Compose.
Мы делим задачи на **UI (Визуал)** и **Data (Данные из JSON)**.

---

## 🤝 Зоны ответственности

### 🎨 UI Developer 
*   **Папка:** `ui`
*   **Задачи:** Верстка, анимации, переходы между экранами.
*   **Инструмент:** Jetpack Compose.
*   **Правило:** Не писать код обработки JSON файлов внутри экранов.

### ⚙️ Logic Developer 
*   **Папка:** `data`
*   **Задачи:** Чтение JSON файлов, фильтрация списков, проверка "логина/пароля" (по базе в JSON).
*   **Инструмент:** Kotlin, Gson/Serialization.
*   **Правило:** Не лезть в `@Composable` функции.

---

## 📂 Структура папок

```text
com.example.MobileApp
│
├── 📂 ui (Моё)
│   ├── 📂 theme        // Цвета и темы
│   ├── 📂 navigation   // Навигация
│   └── 📂 screens      // Экраны (Login, Catalog, Settings)
│
├── 📂 frontback (Стыкуемся)
│   │  Здесь живут ViewModel.
│   │  LoginViewModel.kt, HomeViewModel.kt
│   │
│   └── Эти файлы связывают кнопки UI с логикой данных.
│
└── 📂 data (Твоё)
    ├── 📂 model        // Data classes (Item.kt, User.kt)
    ├── 📂 repository   // Логика (DataRepository.kt)
    └── 📂 assets       // Сами файлы data.json, users.json
```

---

## 🛠 Пример работы (Как связать кнопку и JSON)

### 1. Мой шаг (UI)
В `HomeViewModel.kt` я попрошу у тебя данные:

```kotlin
// HomeViewModel.kt
class HomeViewModel : ViewModel() {
    // Список товаров для экрана
    private val _items = MutableStateFlow<List<Item>>(emptyList())
    val items = _items.asStateFlow()

    fun loadCatalog() {
        // TODO для Напарника:
        // Достань список товаров из json файла и положи в _items
    }
}
```

### 2. Шаг Напарника (Logic)
А ты пишешь класс `Repository`, который читает файл, и заполняет мой2 метод:

```kotlin
// HomeViewModel.kt (Заполняет напарник)
fun loadCatalog() {
    val jsonString = ... // чтение файла
    val list = ... // парсинг JSON
    _items.value = list // Обновление твоего UI
}
```

---

## 🗺 Навигация (Упрощенная)

1.  **Auth Screen**: Экран входа.
2.  **Main App**: Если вошли — показываем каталог и настройки с нижней панелью.

---

## ⚠️ Важно
*   Не работаем в одной ветке `master` одновременно!
*   Все ресурсы (картинки, иконки) кладем в `res/drawable`.